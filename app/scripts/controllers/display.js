'use strict';

/**
 * @ngdoc function
 * @name atompyApp.controller:DisplayCtrl
 * @description
 * # DisplayCtrl
 * Controller of the atompyApp
 */
angular.module('atompyApp')
  .controller('DisplayCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
